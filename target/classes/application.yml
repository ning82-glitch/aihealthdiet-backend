server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aihealthdiet?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

# 安全配置（简化，实际应使用Spring Security）
security:
  basic:
    enabled: false

# CORS配置
cors:
  allowed-origins: "http://localhost:5173,http://localhost:3000"
  allowed-methods: "GET,POST,PUT,DELETE,OPTIONS"
  allowed-headers: "*"
  allow-credentials: true

# AI服务配置
ai:
  vision:
    api:
      key: ""  # 填写你的AI视觉API密钥
      url: "https://api.openai.com/v1/chat/completions"

logging:
  level:
    com.aihealthdiet: DEBUG
    org.springframework.web: INFO